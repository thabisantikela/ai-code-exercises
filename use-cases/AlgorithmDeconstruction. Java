import java.time.LocalDate;
import java.util.ArrayList;
import java.util.List;

public class AlgorithmDeconstruction {

    public enum TaskPriority {
        LOW, MEDIUM, HIGH, URGENT
    }

    public static class Task {
        String title;
        TaskPriority priority;
        LocalDate dueDate;
        boolean isCompleted;

        public Task(String title, TaskPriority priority, LocalDate dueDate, boolean isCompleted) {
            this.title = title;
            this.priority = priority;
            this.dueDate = dueDate;
            this.isCompleted = isCompleted;
        }
    }

    // STEP 3: calculateTaskScore goes here

    // STEP 4: sortTasksByPriority goes here

    // STEP 5: main method goes here

} // end of class
